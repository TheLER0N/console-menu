<Window x:Class="ConsoleMenu.Wpf.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Splash"
        Height="300" Width="520"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Topmost="True">
    <Border CornerRadius="18" Background="#0B0F1A" BorderBrush="#33FFFFFF" BorderThickness="1">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="&#xE7FC;" FontFamily="Segoe MDL2 Assets" FontSize="64"
                       Foreground="#4FA3FF" HorizontalAlignment="Center"/>
            <TextBlock Text="CONSOLE MENU" FontSize="30" Foreground="White"
                       HorizontalAlignment="Center" Margin="0,12,0,4"/>
            <TextBlock Text="Загрузка оболочки..." FontSize="14" Foreground="#9FB3D1"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
</Window>